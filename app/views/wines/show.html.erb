<p id="notice"><%= notice %></p>

<p>
  <strong><h2><%= @wine.name %></h2></strong>
</p>
<p>
  <strong>Strains:</strong>
    <% @wine.blends.each do |blend| %>
      <ul>
        <li><%= "#{blend.strain} (#{blend.percent}%)"%></li>
      </ul>
    <% end %>
</p>

<%= link_to 'Edit', edit_wine_path(@wine) %> |
<%= link_to 'Back', wines_path %>
